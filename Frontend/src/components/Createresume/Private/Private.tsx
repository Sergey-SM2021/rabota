import { Formik } from "formik";
import { FC } from "react";

export const Private: FC = () => {
    return (<Formik
    onSubmit={()=>{}}
    initialValues={{}}>{
        () => (<div>Private</div>)}
    </Formik>)
}

